# nuCAD

This repository combines the original NuCAD frontend and backend into a single Git repository.

## Structure

- `frontend/`: Electron + React frontend
- `backend/`: Express backend

## Notes

- This repository was assembled from two previously separate repositories.
- Large local-only folders such as `node_modules` were intentionally excluded.
